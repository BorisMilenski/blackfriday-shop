package Main;

public class Test {
    public static void main(String[] args) {
        /*PrivilegedAccount DBM = new PrivilegedAccount("DBM", "Tn65z6&dDObh@YJRRt39OwhV");
        ProductList products = DBA.loadProducts(DBM);
        for (int i = 0; i < products.getProducts().size(); i++) {


            Product product = products.getProducts().get(i);
            try {
                ManageSales.executeSale(product, i + 1, products, DBM);
            } catch (QuantityException e) {
                System.out.println(e.getMessage());
            }
        }
        System.out.println(ManageSales.revenueInPeriod(DBM, new GregorianCalendar(2019, 1, 2), new GregorianCalendar(1900 + 2020, 1, 3)));*/
        //DataProtocol.test();
    }
}
